{
  "number": 13808,
  "title": "in depends, cannot request.body() when using UploadFile",
  "body": "### First Check\n\n- [X] I added a very descriptive title here.\n- [X] I used the GitHub search to find a similar question and didn't find it.\n- [X] I searched the FastAPI documentation, with the integrated search.\n- [X] I already searched in Google \"How to X in FastAPI\" and didn't find any information.\n- [X] I already read and followed all the tutorial in the docs and didn't find an answer.\n- [X] I already checked if it is not related to FastAPI but to [Pydantic](https://github.com/pydantic/pydantic).\n- [X] I already checked if it is not related to FastAPI but to [Swagger UI](https://github.com/swagger-api/swagger-ui).\n- [X] I already checked if it is not related to FastAPI but to [ReDoc](https://github.com/Redocly/redoc).\n\n### Commit to Help\n\n- [X] I commit to help with one of those options 👆\n\n### Example Code\n\n```python\nfrom typing import Any\r\n\r\nimport uvicorn\r\nfrom fastapi import Depends, FastAPI, File, Request, UploadFile\r\n\r\napp = FastAPI()\r\n\r\n\r\nasync def credential(request: Request):\r\n    body = await request.body() # RuntimeError: Stream consumed\r\n    # sign(body)\r\n\r\n\r\n@app.post(\"/api\")\r\ndef read_root(\r\n    request: Request,\r\n    credential: Any = Depends(credential),\r\n    file: UploadFile | None = File(None),\r\n):\r\n    return {\"Hello\": \"World\"}\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    uvicorn.run(app, host=\"0.0.0.0\", port=8000)\n```\n\n\n### Description\n\nit raise  `RuntimeError: Stream consumed`,because` request.stream()` called twice, one in` request.form()` and another in r`equest.body()`\r\n\n\n### Operating System\n\nLinux\n\n### Operating System Details\n\n_No response_\n\n### FastAPI Version\n\n0.115.12\n\n### Pydantic Version\n\n2.11.4\n\n### Python Version\n\nPython 3.12.7\n\n### Additional Context\n\n_No response_",
  "body_text": "First Check\n\n I added a very descriptive title here.\n I used the GitHub search to find a similar question and didn't find it.\n I searched the FastAPI documentation, with the integrated search.\n I already searched in Google \"How to X in FastAPI\" and didn't find any information.\n I already read and followed all the tutorial in the docs and didn't find an answer.\n I already checked if it is not related to FastAPI but to Pydantic.\n I already checked if it is not related to FastAPI but to Swagger UI.\n I already checked if it is not related to FastAPI but to ReDoc.\n\nCommit to Help\n\n I commit to help with one of those options 👆\n\nExample Code\nfrom typing import Any\n\nimport uvicorn\nfrom fastapi import Depends, FastAPI, File, Request, UploadFile\n\napp = FastAPI()\n\n\nasync def credential(request: Request):\n    body = await request.body() # RuntimeError: Stream consumed\n    # sign(body)\n\n\n@app.post(\"/api\")\ndef read_root(\n    request: Request,\n    credential: Any = Depends(credential),\n    file: UploadFile | None = File(None),\n):\n    return {\"Hello\": \"World\"}\n\n\nif __name__ == \"__main__\":\n    uvicorn.run(app, host=\"0.0.0.0\", port=8000)\nDescription\nit raise  RuntimeError: Stream consumed,because request.stream() called twice, one in request.form() and another in request.body()\nOperating System\nLinux\nOperating System Details\nNo response\nFastAPI Version\n0.115.12\nPydantic Version\n2.11.4\nPython Version\nPython 3.12.7\nAdditional Context\nNo response",
  "created_at": "2025-06-19T14:13:25Z",
  "url": "https://github.com/fastapi/fastapi/discussions/13808",
  "answered": true,
  "answer": {
    "body": "Hi, since you're using an UploadFile, FastAPI/Starlette will interpret and store your post data as a multipart form. This is done internally via `await request.form()` as soon as the request is received. After that, the raw request data is no longer available and therefore can not be accessed via `await request.body()`.\r\n\r\nIronically, if the post data is first accessed via `await request.body()` it is possible to access it again via `await request.form()`, because the raw request data can be reinterpreted as multipart form data.\r\n\r\nThis leaves us with three options:\r\n\r\n1. Option: work with form data in your `credential` dependency\r\n    \r\n    ```python\r\n    async def credential(request: Request):\r\n        form = await request.form()\r\n    ```\r\n\r\n2. Option: interpret the post data yourself\r\n\r\n    ```python\r\n    async def credential(request: Request):\r\n        body = await request.body()  # No longer raises a runtime error\r\n    \r\n    \r\n    @app.post(\"/api\")\r\n    async def read_root(\r\n        request: Request,\r\n        credential: Any = Depends(credential),\r\n    ):\r\n        form = await request.form()\r\n        file = form.get(\"file\")\r\n        return {\"Hello\": file.filename if isinstance(file, UploadFile) else \"no file\"}\r\n    ```\r\n\r\n3. Option: start a discussion in the Starlette repo\r\n    I'm pretty sure it's a performance optimization that the raw post data is no longer available if it has been accessed via `await request.form()` first. However, technically, it would be possible to preserve the raw post data in that case.",
    "body_text": "Hi, since you're using an UploadFile, FastAPI/Starlette will interpret and store your post data as a multipart form. This is done internally via await request.form() as soon as the request is received. After that, the raw request data is no longer available and therefore can not be accessed via await request.body().\nIronically, if the post data is first accessed via await request.body() it is possible to access it again via await request.form(), because the raw request data can be reinterpreted as multipart form data.\nThis leaves us with three options:\n\n\nOption: work with form data in your credential dependency\nasync def credential(request: Request):\n    form = await request.form()\n\n\nOption: interpret the post data yourself\nasync def credential(request: Request):\n    body = await request.body()  # No longer raises a runtime error\n\n\n@app.post(\"/api\")\nasync def read_root(\n    request: Request,\n    credential: Any = Depends(credential),\n):\n    form = await request.form()\n    file = form.get(\"file\")\n    return {\"Hello\": file.filename if isinstance(file, UploadFile) else \"no file\"}\n\n\nOption: start a discussion in the Starlette repo\nI'm pretty sure it's a performance optimization that the raw post data is no longer available if it has been accessed via await request.form() first. However, technically, it would be possible to preserve the raw post data in that case.",
    "url": "https://github.com/fastapi/fastapi/discussions/13808#discussioncomment-13522274",
    "created_at": "2025-06-19T16:40:33Z",
    "upvotes": 1,
    "author": "DoctorJohn",
    "author_association": "MEMBER",
    "is_maintainer": true
  }
}