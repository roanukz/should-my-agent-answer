{
  "number": 13165,
  "title": "Passing `selectinload(...)` from SQLAlchemy into dependency as an attribute raises `AttributeError: 'Load' object has no attribute 'read'`",
  "body": "### First Check\n\n- [X] I added a very descriptive title here.\n- [x] I used the GitHub search to find a similar question and didn't find it.\n- [X] I searched the FastAPI documentation, with the integrated search.\n- [X] I already searched in Google \"How to X in FastAPI\" and didn't find any information.\n- [X] I already read and followed all the tutorial in the docs and didn't find an answer.\n- [X] I already checked if it is not related to FastAPI but to [Pydantic](https://github.com/pydantic/pydantic).\n- [X] I already checked if it is not related to FastAPI but to [Swagger UI](https://github.com/swagger-api/swagger-ui).\n- [X] I already checked if it is not related to FastAPI but to [ReDoc](https://github.com/Redocly/redoc).\n\n### Commit to Help\n\n- [X] I commit to help with one of those options 👆\n\n### Example Code\n\n```python\nfrom typing import Callable\r\nfrom functools import partial\r\n\r\nimport uvicorn\r\nfrom fastapi import FastAPI, Depends, HTTPException\r\nfrom sqlalchemy import Column, Integer, String, ForeignKey, select\r\nfrom sqlalchemy.orm import DeclarativeBase, selectinload, relationship, Mapped\r\nfrom sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker\r\nfrom pydantic import BaseModel, ConfigDict\r\n\r\nMAIN_DATABASE_URL = \"sqlite+aiosqlite:///:memory:\"\r\nengine = create_async_engine(MAIN_DATABASE_URL, echo=True, future=True)\r\nasync_session = async_sessionmaker(engine, expire_on_commit=False)\r\n\r\n\r\nasync def init_db(session: AsyncSession):\r\n    role1 = Role(name=\"role1\")\r\n    role2 = Role(name=\"role2\")\r\n    session.add_all([role1, role2])\r\n\r\n    profile1 = Profile(name=\"profile1\", role=role1)\r\n    profile2 = Profile(name=\"profile2\", role=role2)\r\n    session.add_all([profile1, profile2])\r\n\r\n    await session.commit()\r\n\r\nasync def lifespan(app: FastAPI):\r\n    async with engine.begin() as conn:\r\n        await conn.run_sync(Base.metadata.create_all)\r\n    async with async_session() as session:\r\n        await init_db(session)\r\n    yield\r\n\r\n\r\napp = FastAPI()\r\n\r\nclass Base(DeclarativeBase):\r\n    pass\r\n\r\nasync def get_db():\r\n    async with async_session() as session:\r\n        yield session\r\n\r\nclass Role(Base):\r\n    __tablename__ = \"roles\"\r\n    id = Column(Integer, primary_key=True, index=True)\r\n    name = Column(String, unique=True)\r\n    profiles: Mapped[list[\"Profile\"]] = relationship(\"Profile\", back_populates=\"role\")\r\n\r\nclass Profile(Base):\r\n    __tablename__ = \"profiles\"\r\n    id = Column(Integer, primary_key=True, index=True)\r\n    role_id = Column(Integer, ForeignKey(\"roles.id\"))\r\n    name = Column(String, unique=True)\r\n    role: Mapped[Role] = relationship(\"Role\", back_populates=\"profiles\")\r\n\r\n\r\n\r\n\r\nclass RoleInDB(BaseModel):\r\n    model_config = ConfigDict(from_attributes=True)\r\n    id: int\r\n    name: str\r\n\r\nclass ProfileInDB(BaseModel):\r\n    model_config = ConfigDict(from_attributes=True)\r\n    id: int\r\n    name: str\r\n    role: RoleInDB\r\n\r\n\r\nasync def get_current_profile(\r\n    db: AsyncSession = Depends(get_db),\r\n    profile_id: int | None = None,\r\n    load_options: list | None = None\r\n) -> Profile:\r\n    if not load_options:\r\n        load_options = []\r\n\r\n    profile = await db.scalar(\r\n        select(Profile).filter(Profile.id == profile_id).options(*load_options)\r\n    )\r\n    if not profile:\r\n        raise HTTPException(status_code=404, detail=\"Profile not found\")\r\n    return profile\r\n\r\n\r\ndef get_profile_load_options(*load_options) -> Callable:\r\n    print(f\"UTILS get_profile_load_options {load_options=}\")\r\n    return partial(get_current_profile, load_options=list(load_options))\r\n\r\n\r\n@app.get(\"/profiles/me\")\r\nasync def read_profile_by_id(\r\n    current_profile: Profile = Depends(\r\n        get_profile_load_options(selectinload(Profile.role))  # most likely this raises an error\r\n    ),\r\n) -> ProfileInDB:\r\n    return ProfileInDB.model_validate(current_profile)\r\n\r\nif __name__ == \"__main__\":\r\n    uvicorn.run(app, host=\"127.0.0.1\", port=8000)\n```\n\n\n### Description\n\nI want to pass load options (joinedload, selectinload) into dependency to specify \"what models do I want\" and then use them in my class method (my models inherit from my own base class with additional methods). Also, I checked this utils.py file, and indeed VS Code highlights it with white color, meaning not defined.\n\n### Operating System\n\nWindows\n\n### Operating System Details\n\n_No response_\n\n### FastAPI Version\n\n0.115.5\n\n### Pydantic Version\n\n2.9.2\n\n### Python Version\n\n3.13.0\n\n### Additional Context\n\nI am migrating the project to async SQLAlchemy 2.0, which requires explicitly requesting related models, not as in the default (sync) version by just `profile.role`, as it raises an error.",
  "body_text": "First Check\n\n I added a very descriptive title here.\n I used the GitHub search to find a similar question and didn't find it.\n I searched the FastAPI documentation, with the integrated search.\n I already searched in Google \"How to X in FastAPI\" and didn't find any information.\n I already read and followed all the tutorial in the docs and didn't find an answer.\n I already checked if it is not related to FastAPI but to Pydantic.\n I already checked if it is not related to FastAPI but to Swagger UI.\n I already checked if it is not related to FastAPI but to ReDoc.\n\nCommit to Help\n\n I commit to help with one of those options 👆\n\nExample Code\nfrom typing import Callable\nfrom functools import partial\n\nimport uvicorn\nfrom fastapi import FastAPI, Depends, HTTPException\nfrom sqlalchemy import Column, Integer, String, ForeignKey, select\nfrom sqlalchemy.orm import DeclarativeBase, selectinload, relationship, Mapped\nfrom sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker\nfrom pydantic import BaseModel, ConfigDict\n\nMAIN_DATABASE_URL = \"sqlite+aiosqlite:///:memory:\"\nengine = create_async_engine(MAIN_DATABASE_URL, echo=True, future=True)\nasync_session = async_sessionmaker(engine, expire_on_commit=False)\n\n\nasync def init_db(session: AsyncSession):\n    role1 = Role(name=\"role1\")\n    role2 = Role(name=\"role2\")\n    session.add_all([role1, role2])\n\n    profile1 = Profile(name=\"profile1\", role=role1)\n    profile2 = Profile(name=\"profile2\", role=role2)\n    session.add_all([profile1, profile2])\n\n    await session.commit()\n\nasync def lifespan(app: FastAPI):\n    async with engine.begin() as conn:\n        await conn.run_sync(Base.metadata.create_all)\n    async with async_session() as session:\n        await init_db(session)\n    yield\n\n\napp = FastAPI()\n\nclass Base(DeclarativeBase):\n    pass\n\nasync def get_db():\n    async with async_session() as session:\n        yield session\n\nclass Role(Base):\n    __tablename__ = \"roles\"\n    id = Column(Integer, primary_key=True, index=True)\n    name = Column(String, unique=True)\n    profiles: Mapped[list[\"Profile\"]] = relationship(\"Profile\", back_populates=\"role\")\n\nclass Profile(Base):\n    __tablename__ = \"profiles\"\n    id = Column(Integer, primary_key=True, index=True)\n    role_id = Column(Integer, ForeignKey(\"roles.id\"))\n    name = Column(String, unique=True)\n    role: Mapped[Role] = relationship(\"Role\", back_populates=\"profiles\")\n\n\n\n\nclass RoleInDB(BaseModel):\n    model_config = ConfigDict(from_attributes=True)\n    id: int\n    name: str\n\nclass ProfileInDB(BaseModel):\n    model_config = ConfigDict(from_attributes=True)\n    id: int\n    name: str\n    role: RoleInDB\n\n\nasync def get_current_profile(\n    db: AsyncSession = Depends(get_db),\n    profile_id: int | None = None,\n    load_options: list | None = None\n) -> Profile:\n    if not load_options:\n        load_options = []\n\n    profile = await db.scalar(\n        select(Profile).filter(Profile.id == profile_id).options(*load_options)\n    )\n    if not profile:\n        raise HTTPException(status_code=404, detail=\"Profile not found\")\n    return profile\n\n\ndef get_profile_load_options(*load_options) -> Callable:\n    print(f\"UTILS get_profile_load_options {load_options=}\")\n    return partial(get_current_profile, load_options=list(load_options))\n\n\n@app.get(\"/profiles/me\")\nasync def read_profile_by_id(\n    current_profile: Profile = Depends(\n        get_profile_load_options(selectinload(Profile.role))  # most likely this raises an error\n    ),\n) -> ProfileInDB:\n    return ProfileInDB.model_validate(current_profile)\n\nif __name__ == \"__main__\":\n    uvicorn.run(app, host=\"127.0.0.1\", port=8000)\nDescription\nI want to pass load options (joinedload, selectinload) into dependency to specify \"what models do I want\" and then use them in my class method (my models inherit from my own base class with additional methods). Also, I checked this utils.py file, and indeed VS Code highlights it with white color, meaning not defined.\nOperating System\nWindows\nOperating System Details\nNo response\nFastAPI Version\n0.115.5\nPydantic Version\n2.9.2\nPython Version\n3.13.0\nAdditional Context\nI am migrating the project to async SQLAlchemy 2.0, which requires explicitly requesting related models, not as in the default (sync) version by just profile.role, as it raises an error.",
  "created_at": "2025-01-07T16:11:20Z",
  "url": "https://github.com/fastapi/fastapi/discussions/13165",
  "answered": true,
  "answer": {
    "body": "This is because `partial` with `async` functions isn't supported in `Depends`: issue https://github.com/fastapi/fastapi/issues/10720\r\n\r\nYou can workaround it by rewriting `get_profile_load_options` like this:\r\n```python\r\ndef get_profile_load_options(*load_options) -> Callable:\r\n    print(f\"UTILS get_profile_load_options {load_options=}\")\r\n\r\n    async def parametrized(\r\n        db: AsyncSession = Depends(get_db),\r\n        profile_id: int | None = None,\r\n    ) -> Profile:\r\n        return await get_current_profile(\r\n            db=db, profile_id=profile_id, load_options=list(load_options)\r\n        )\r\n    return parametrized\r\n```\r\nOr this:\r\n```python\r\ndef get_profile_load_options(*load_options):\r\n    print(f\"UTILS get_profile_load_options {load_options=}\")\r\n\r\n    async def get_current_profile(\r\n        db: AsyncSession = Depends(get_db),\r\n        profile_id: int | None = None,\r\n    ) -> Profile:\r\n        nonlocal load_options\r\n        if not load_options:\r\n            load_options = []\r\n\r\n        profile = await db.scalar(\r\n            select(Profile).filter(Profile.id == profile_id).options(*load_options)\r\n        )\r\n        if not profile:\r\n            raise HTTPException(status_code=404, detail=\"Profile not found\")\r\n        return profile\r\n\r\n    return get_current_profile\r\n```\r\n",
    "body_text": "This is because partial with async functions isn't supported in Depends: issue #10720\nYou can workaround it by rewriting get_profile_load_options like this:\ndef get_profile_load_options(*load_options) -> Callable:\n    print(f\"UTILS get_profile_load_options {load_options=}\")\n\n    async def parametrized(\n        db: AsyncSession = Depends(get_db),\n        profile_id: int | None = None,\n    ) -> Profile:\n        return await get_current_profile(\n            db=db, profile_id=profile_id, load_options=list(load_options)\n        )\n    return parametrized\nOr this:\ndef get_profile_load_options(*load_options):\n    print(f\"UTILS get_profile_load_options {load_options=}\")\n\n    async def get_current_profile(\n        db: AsyncSession = Depends(get_db),\n        profile_id: int | None = None,\n    ) -> Profile:\n        nonlocal load_options\n        if not load_options:\n            load_options = []\n\n        profile = await db.scalar(\n            select(Profile).filter(Profile.id == profile_id).options(*load_options)\n        )\n        if not profile:\n            raise HTTPException(status_code=404, detail=\"Profile not found\")\n        return profile\n\n    return get_current_profile",
    "url": "https://github.com/fastapi/fastapi/discussions/13165#discussioncomment-13232396",
    "created_at": "2025-05-22T09:36:34Z",
    "upvotes": 1,
    "author": "YuriiMotov",
    "author_association": "MEMBER",
    "is_maintainer": true
  }
}