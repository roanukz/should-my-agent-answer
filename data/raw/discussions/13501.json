{
  "number": 13501,
  "title": "websocket receive timeout field missing",
  "body": "### First Check\r\n\r\n- [x] I added a very descriptive title here.\r\n- [X] I used the GitHub search to find a similar question and didn't find it.\r\n- [X] I searched the FastAPI documentation, with the integrated search.\r\n- [X] I already searched in Google \"How to X in FastAPI\" and didn't find any information.\r\n- [X] I already read and followed all the tutorial in the docs and didn't find an answer.\r\n- [X] I already checked if it is not related to FastAPI but to [Pydantic](https://github.com/pydantic/pydantic).\r\n- [X] I already checked if it is not related to FastAPI but to [Swagger UI](https://github.com/swagger-api/swagger-ui).\r\n- [X] I already checked if it is not related to FastAPI but to [ReDoc](https://github.com/Redocly/redoc).\r\n\r\n### Commit to Help\r\n\r\n- [X] I commit to help with one of those options 👆\r\n\r\n### Example Code\r\n\r\n```python\r\nfrom fastapi import FastAPI, WebSocket, WebSocketDisconnect\r\nfrom fastapi.testclient import TestClient\r\n\r\nimport asyncio\r\n\r\napp = FastAPI()\r\n\r\n\r\n@app.websocket(\"/ws\")\r\nasync def websocket_endpoint(websocket: WebSocket):\r\n    await websocket.accept()\r\n    try:\r\n        while True:\r\n            text = await websocket.receive_text()\r\n            if text == \"action\":\r\n                await websocket.send_text(\"done\")\r\n    except WebSocketDisconnect:\r\n        return await websocket.close()\r\n\r\n\r\nasync def test_websocket_timeout():\r\n    client = TestClient(app)\r\n    with client.websocket_connect(\"/ws\") as ws:\r\n        ws.send_text(\"action\")\r\n        assert ws.receive_text() == \"done\"\r\n        ws.send_text(\"not-action\")\r\n        # freezes without timing out\r\n        ws.receive_text()\r\n        # still freezes without timing out\r\n        assert await asyncio.wait_for(ws.receive_text(), 1.0) != \"done\"\r\n```\r\n\r\n\r\n### Description\r\n\r\nI'm trying to implement a timeout for the websocket receive_{text, bytes, json} functions, but it doesn't work no matter what. Even with asyncio.wait_for it still freezes on ws.receive_text and waits for an answer. I'm trying to use this in testing so I can test if other connections are not receiving the secret information. Any help is greatly appreciated.\r\n\r\n### Operating System\r\n\r\nWindows\r\n\r\n### Operating System Details\r\n\r\nWindows 10\r\n\r\n### FastAPI Version\r\n\r\n0.115.11\r\n\r\n### Pydantic Version\r\n\r\n2.10.6\r\n\r\n### Python Version\r\n\r\n3.13.1\r\n\r\n### Additional Context\r\n\r\n_No response_",
  "body_text": "First Check\n\n I added a very descriptive title here.\n I used the GitHub search to find a similar question and didn't find it.\n I searched the FastAPI documentation, with the integrated search.\n I already searched in Google \"How to X in FastAPI\" and didn't find any information.\n I already read and followed all the tutorial in the docs and didn't find an answer.\n I already checked if it is not related to FastAPI but to Pydantic.\n I already checked if it is not related to FastAPI but to Swagger UI.\n I already checked if it is not related to FastAPI but to ReDoc.\n\nCommit to Help\n\n I commit to help with one of those options 👆\n\nExample Code\nfrom fastapi import FastAPI, WebSocket, WebSocketDisconnect\nfrom fastapi.testclient import TestClient\n\nimport asyncio\n\napp = FastAPI()\n\n\n@app.websocket(\"/ws\")\nasync def websocket_endpoint(websocket: WebSocket):\n    await websocket.accept()\n    try:\n        while True:\n            text = await websocket.receive_text()\n            if text == \"action\":\n                await websocket.send_text(\"done\")\n    except WebSocketDisconnect:\n        return await websocket.close()\n\n\nasync def test_websocket_timeout():\n    client = TestClient(app)\n    with client.websocket_connect(\"/ws\") as ws:\n        ws.send_text(\"action\")\n        assert ws.receive_text() == \"done\"\n        ws.send_text(\"not-action\")\n        # freezes without timing out\n        ws.receive_text()\n        # still freezes without timing out\n        assert await asyncio.wait_for(ws.receive_text(), 1.0) != \"done\"\nDescription\nI'm trying to implement a timeout for the websocket receive_{text, bytes, json} functions, but it doesn't work no matter what. Even with asyncio.wait_for it still freezes on ws.receive_text and waits for an answer. I'm trying to use this in testing so I can test if other connections are not receiving the secret information. Any help is greatly appreciated.\nOperating System\nWindows\nOperating System Details\nWindows 10\nFastAPI Version\n0.115.11\nPydantic Version\n2.10.6\nPython Version\n3.13.1\nAdditional Context\nNo response",
  "created_at": "2025-03-19T08:15:39Z",
  "url": "https://github.com/fastapi/fastapi/discussions/13501",
  "answered": true,
  "answer": {
    "body": "Hello @zmn-hamid,\r\n\r\nTestClient is from Starlette, so any improvements or suggestions should probably be addressed there.\r\n\r\nYour test example is freezing because `receive_text()` is synchronous and will not be interrupted by `wait_for`.\r\n\r\nSomething like the following should work in your case:\r\n``` python\r\n\r\n@pytest.mark.asyncio\r\nasync def test_websocket_timeout():\r\n    with client.websocket_connect(\"/ws\") as ws:\r\n        ws.send_text(\"action\")\r\n        assert ws.receive_text() == \"done\"\r\n        ws.send_text(\"not-action\")\r\n        with pytest.raises(asyncio.TimeoutError):\r\n            await asyncio.wait_for(run_in_threadpool(ws.receive_text), 1.0)\r\n```\r\n\r\nIn general, I think it would be better to use an external WebSocket library for testing. I also found a related discussion on Starlette that might be relevant (https://github.com/encode/starlette/discussions/2195).",
    "body_text": "Hello @zmn-hamid,\nTestClient is from Starlette, so any improvements or suggestions should probably be addressed there.\nYour test example is freezing because receive_text() is synchronous and will not be interrupted by wait_for.\nSomething like the following should work in your case:\n@pytest.mark.asyncio\nasync def test_websocket_timeout():\n    with client.websocket_connect(\"/ws\") as ws:\n        ws.send_text(\"action\")\n        assert ws.receive_text() == \"done\"\n        ws.send_text(\"not-action\")\n        with pytest.raises(asyncio.TimeoutError):\n            await asyncio.wait_for(run_in_threadpool(ws.receive_text), 1.0)\nIn general, I think it would be better to use an external WebSocket library for testing. I also found a related discussion on Starlette that might be relevant (Kludex/starlette#2195).",
    "url": "https://github.com/fastapi/fastapi/discussions/13501#discussioncomment-12557940",
    "created_at": "2025-03-19T23:36:21Z",
    "upvotes": 2,
    "author": "luzzodev",
    "author_association": "CONTRIBUTOR",
    "is_maintainer": false
  }
}