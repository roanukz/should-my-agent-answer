{
  "number": 13277,
  "title": "Db Retry mechanism in case of failure while connecting with db.",
  "body": "### First Check\n\n- [X] I added a very descriptive title here.\n- [X] I used the GitHub search to find a similar question and didn't find it.\n- [X] I searched the FastAPI documentation, with the integrated search.\n- [X] I already searched in Google \"How to X in FastAPI\" and didn't find any information.\n- [X] I already read and followed all the tutorial in the docs and didn't find an answer.\n- [X] I already checked if it is not related to FastAPI but to [Pydantic](https://github.com/pydantic/pydantic).\n- [X] I already checked if it is not related to FastAPI but to [Swagger UI](https://github.com/swagger-api/swagger-ui).\n- [X] I already checked if it is not related to FastAPI but to [ReDoc](https://github.com/Redocly/redoc).\n\n### Commit to Help\n\n- [X] I commit to help with one of those options 👆\n\n### Example Code\n\n```python\nfrom fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm\r\nfrom sqlalchemy.orm import joinedload\r\nfrom sqlalchemy import select\r\nfrom models.user_model import IftApplication, IftPage, IftRole, IftRoleApplication, IftUser, IftUserRole, get_db\r\nfrom user.userschema import ApplicationCreate, ApplicationResponse, CompleteApplicationResponse, CompletePageResponse, PageCreate, PageResponse, PageUpdate, ResponseError, RoleApplicationCreate, RoleApplicationResponse, RoleCreate, RoleResponse, RoleUpdate, UserCreate, UserResponse, UserRoleCreate, UserRoleResponse, UserUpdate\r\nfrom fastapi import APIRouter, Depends, HTTPException, Request\r\nfrom pathlib import Path\r\nfrom sqlalchemy.ext.asyncio import AsyncSession\r\nfrom typing import List, Union\r\nfrom sqlalchemy.exc import NoResultFound\r\nfrom passlib.context import CryptContext\r\nfrom pydantic import BaseModel, EmailStr, field_validator\r\nfrom sqlalchemy import Column, Integer, String, Boolean, Enum, DateTime\r\nfrom sqlalchemy.ext.declarative import declarative_base\r\nfrom datetime import datetime, timedelta\r\nimport pytz\r\nfrom jose import JWTError, jwt\r\nfrom config import constant\r\n\r\nALGORITHM = \"HS256\"\r\nSECRET_KEY = constant.get(\"SECRET\")\r\nuserroute = APIRouter(prefix=\"/users\", tags=[\"UMS\"])\r\nROOT_DIR_PROJECT = Path(__file__).parent\r\nprint(ROOT_DIR_PROJECT)\r\nACCESS_TOKEN_EXPIRE_MINUTES = constant.get(\r\n    \"boportal\").get(\"ACCESS_TOKEN_EXPIRE_MINUTES\")\r\nUTC = pytz.utc\r\nIST = pytz.timezone(\"Asia/Kolkata\")\r\npwd_context = CryptContext(schemes=[\"bcrypt\"], deprecated=\"auto\")\r\noauth2_scheme = OAuth2PasswordBearer(tokenUrl=\"/users/login\")\r\n\r\n\r\nasync def get_current_user(token: str = Depends(oauth2_scheme), request: Request = None):\r\n    payload = await verify_access_token(token)\r\n    client_ip = request.client.host\r\n    print(f\"Request from IP: {client_ip}\")\r\n    if not payload:\r\n        raise HTTPException(\r\n            status_code=401,\r\n            detail=f\"Token is invalid or expired. Request from IP: {client_ip}\",\r\n        )\r\n\r\n    return payload\r\n\r\n\r\nasync def get_admin_user(token: str = Depends(oauth2_scheme), db: AsyncSession = Depends(get_db)):\r\n    payload = await verify_access_token(token)\r\n    if not payload:\r\n        raise HTTPException(\r\n            status_code=401, detail=\"Token is invalid or expired\"\r\n        )\r\n    result = await db.execute(select(IftUser).filter(IftUser.email == payload[\"sub\"]))\r\n    user = result.scalars().first()\r\n    if str(user.role).lower() != 'admin':\r\n        raise HTTPException(\r\n            status_code=401, detail=\"Token is invalid or expired\"\r\n        )\r\n    return payload\r\n\r\n\r\n@userroute.post(\"/login\", include_in_schema=True, description=\"Login to the application\", response_model=dict)\r\nasync def login_user(form_data: OAuth2PasswordRequestForm = Depends(), db: AsyncSession = Depends(get_db)):\r\n    user_data = await db.execute(select(IftUser).filter(IftUser.email == form_data.username))\r\n    user = user_data.scalars().first()\r\n    if not user or not verify_password(form_data.password, user.password_hash):\r\n        raise HTTPException(status_code=401, detail=\"Invalid credentials\")\r\n    token_data = {\"sub\": user.email, \"user_name\": user.name, \"access\": \"\"}\r\n    access_token = await create_access_token(data=token_data)\r\n    return {\"access_token\": access_token, \"token_type\": \"bearer\"}\r\n\r\n\r\nAnother python file having db connections \r\nfrom asyncio import sleep\r\nfrom fastapi import FastAPI, HTTPException\r\nfrom sqlalchemy.exc import OperationalError\r\nfrom sqlalchemy.ext.asyncio import AsyncSession\r\nfrom config import constant\r\nfrom datetime import datetime\r\nfrom sqlalchemy.ext.declarative import declarative_base\r\nfrom sqlalchemy.orm import relationship\r\nfrom sqlalchemy import (\r\n    JSON, Boolean, Column, Enum, PrimaryKeyConstraint, String, Integer, ForeignKey, TIMESTAMP, text, UniqueConstraint\r\n)\r\nfrom sqlalchemy import TIMESTAMP, Column, String, ForeignKey, func, DateTime\r\nfrom sqlalchemy.orm import relationship, Mapped, mapped_column\r\nimport pytz\r\nfrom sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine\r\nfrom sqlalchemy.orm import DeclarativeBase\r\n\r\nUTC = pytz.utc\r\nIST = pytz.timezone(\"Asia/Kolkata\")\r\n\r\ndb_url = \"mysql+aiomysql://root:my@127.0.0.1:3306/ne\"\r\n\r\nengine = create_async_engine(\r\n            db_url, pool_size=30, max_overflow=5, pool_recycle=600, pool_pre_ping=True, echo=True)\r\n        \r\nSessionlocal = async_sessionmaker(\r\n    autoflush=False, autocommit=False, bind=engine, expire_on_commit=False\r\n)\r\n\r\nclass Base(DeclarativeBase):\r\n    pass\r\n\r\nasync def get_db():\r\n    # async with engine.begin() as conn:\r\n    # await conn.run_sync(Base.metadata.create_all)\r\n    async with Sessionlocal() as db:\r\n        try:\r\n            yield db\r\n        except Exception as e:\r\n            await db.rollback()  # Rollback on error\r\n            raise e\r\n        finally:\r\n            await db.close()  # Ensures connection is returned to pool\n```\n\n\n### Description\n\nI have given an example of my code structure. i am looking out for a solution where when i am trying to logina nd while login if there is any error in db then it should retry again without letting the user that i have retried. a mechnism for db retry which i can integrate in all the api's.\n\n### Operating System\n\nmacOS\n\n### Operating System Details\n\n_No response_\n\n### FastAPI Version\n\nfastapi==0.105.0\n\n### Pydantic Version\n\npydantic==2.5.2 pydantic_core==2.14.5\n\n### Python Version\n\n3.10\n\n### Additional Context\n\n_No response_",
  "body_text": "First Check\n\n I added a very descriptive title here.\n I used the GitHub search to find a similar question and didn't find it.\n I searched the FastAPI documentation, with the integrated search.\n I already searched in Google \"How to X in FastAPI\" and didn't find any information.\n I already read and followed all the tutorial in the docs and didn't find an answer.\n I already checked if it is not related to FastAPI but to Pydantic.\n I already checked if it is not related to FastAPI but to Swagger UI.\n I already checked if it is not related to FastAPI but to ReDoc.\n\nCommit to Help\n\n I commit to help with one of those options 👆\n\nExample Code\nfrom fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm\nfrom sqlalchemy.orm import joinedload\nfrom sqlalchemy import select\nfrom models.user_model import IftApplication, IftPage, IftRole, IftRoleApplication, IftUser, IftUserRole, get_db\nfrom user.userschema import ApplicationCreate, ApplicationResponse, CompleteApplicationResponse, CompletePageResponse, PageCreate, PageResponse, PageUpdate, ResponseError, RoleApplicationCreate, RoleApplicationResponse, RoleCreate, RoleResponse, RoleUpdate, UserCreate, UserResponse, UserRoleCreate, UserRoleResponse, UserUpdate\nfrom fastapi import APIRouter, Depends, HTTPException, Request\nfrom pathlib import Path\nfrom sqlalchemy.ext.asyncio import AsyncSession\nfrom typing import List, Union\nfrom sqlalchemy.exc import NoResultFound\nfrom passlib.context import CryptContext\nfrom pydantic import BaseModel, EmailStr, field_validator\nfrom sqlalchemy import Column, Integer, String, Boolean, Enum, DateTime\nfrom sqlalchemy.ext.declarative import declarative_base\nfrom datetime import datetime, timedelta\nimport pytz\nfrom jose import JWTError, jwt\nfrom config import constant\n\nALGORITHM = \"HS256\"\nSECRET_KEY = constant.get(\"SECRET\")\nuserroute = APIRouter(prefix=\"/users\", tags=[\"UMS\"])\nROOT_DIR_PROJECT = Path(__file__).parent\nprint(ROOT_DIR_PROJECT)\nACCESS_TOKEN_EXPIRE_MINUTES = constant.get(\n    \"boportal\").get(\"ACCESS_TOKEN_EXPIRE_MINUTES\")\nUTC = pytz.utc\nIST = pytz.timezone(\"Asia/Kolkata\")\npwd_context = CryptContext(schemes=[\"bcrypt\"], deprecated=\"auto\")\noauth2_scheme = OAuth2PasswordBearer(tokenUrl=\"/users/login\")\n\n\nasync def get_current_user(token: str = Depends(oauth2_scheme), request: Request = None):\n    payload = await verify_access_token(token)\n    client_ip = request.client.host\n    print(f\"Request from IP: {client_ip}\")\n    if not payload:\n        raise HTTPException(\n            status_code=401,\n            detail=f\"Token is invalid or expired. Request from IP: {client_ip}\",\n        )\n\n    return payload\n\n\nasync def get_admin_user(token: str = Depends(oauth2_scheme), db: AsyncSession = Depends(get_db)):\n    payload = await verify_access_token(token)\n    if not payload:\n        raise HTTPException(\n            status_code=401, detail=\"Token is invalid or expired\"\n        )\n    result = await db.execute(select(IftUser).filter(IftUser.email == payload[\"sub\"]))\n    user = result.scalars().first()\n    if str(user.role).lower() != 'admin':\n        raise HTTPException(\n            status_code=401, detail=\"Token is invalid or expired\"\n        )\n    return payload\n\n\n@userroute.post(\"/login\", include_in_schema=True, description=\"Login to the application\", response_model=dict)\nasync def login_user(form_data: OAuth2PasswordRequestForm = Depends(), db: AsyncSession = Depends(get_db)):\n    user_data = await db.execute(select(IftUser).filter(IftUser.email == form_data.username))\n    user = user_data.scalars().first()\n    if not user or not verify_password(form_data.password, user.password_hash):\n        raise HTTPException(status_code=401, detail=\"Invalid credentials\")\n    token_data = {\"sub\": user.email, \"user_name\": user.name, \"access\": \"\"}\n    access_token = await create_access_token(data=token_data)\n    return {\"access_token\": access_token, \"token_type\": \"bearer\"}\n\n\nAnother python file having db connections \nfrom asyncio import sleep\nfrom fastapi import FastAPI, HTTPException\nfrom sqlalchemy.exc import OperationalError\nfrom sqlalchemy.ext.asyncio import AsyncSession\nfrom config import constant\nfrom datetime import datetime\nfrom sqlalchemy.ext.declarative import declarative_base\nfrom sqlalchemy.orm import relationship\nfrom sqlalchemy import (\n    JSON, Boolean, Column, Enum, PrimaryKeyConstraint, String, Integer, ForeignKey, TIMESTAMP, text, UniqueConstraint\n)\nfrom sqlalchemy import TIMESTAMP, Column, String, ForeignKey, func, DateTime\nfrom sqlalchemy.orm import relationship, Mapped, mapped_column\nimport pytz\nfrom sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine\nfrom sqlalchemy.orm import DeclarativeBase\n\nUTC = pytz.utc\nIST = pytz.timezone(\"Asia/Kolkata\")\n\ndb_url = \"mysql+aiomysql://root:my@127.0.0.1:3306/ne\"\n\nengine = create_async_engine(\n            db_url, pool_size=30, max_overflow=5, pool_recycle=600, pool_pre_ping=True, echo=True)\n        \nSessionlocal = async_sessionmaker(\n    autoflush=False, autocommit=False, bind=engine, expire_on_commit=False\n)\n\nclass Base(DeclarativeBase):\n    pass\n\nasync def get_db():\n    # async with engine.begin() as conn:\n    # await conn.run_sync(Base.metadata.create_all)\n    async with Sessionlocal() as db:\n        try:\n            yield db\n        except Exception as e:\n            await db.rollback()  # Rollback on error\n            raise e\n        finally:\n            await db.close()  # Ensures connection is returned to pool\nDescription\nI have given an example of my code structure. i am looking out for a solution where when i am trying to logina nd while login if there is any error in db then it should retry again without letting the user that i have retried. a mechnism for db retry which i can integrate in all the api's.\nOperating System\nmacOS\nOperating System Details\nNo response\nFastAPI Version\nfastapi==0.105.0\nPydantic Version\npydantic==2.5.2 pydantic_core==2.14.5\nPython Version\n3.10\nAdditional Context\nNo response",
  "created_at": "2025-01-29T16:25:26Z",
  "url": "https://github.com/fastapi/fastapi/discussions/13277",
  "answered": true,
  "answer": {
    "body": "Did you try something like 👇 ?\r\n\r\n```python\r\ntry:\r\n    user_data = await db.execute(select(IftUser).filter(IftUser.email == form_data.username))\r\nexcept:\r\n    # retry code\r\n```\r\n\r\nof course you should filter only the exceptions you want to catch in except block.",
    "body_text": "Did you try something like 👇 ?\ntry:\n    user_data = await db.execute(select(IftUser).filter(IftUser.email == form_data.username))\nexcept:\n    # retry code\nof course you should filter only the exceptions you want to catch in except block.",
    "url": "https://github.com/fastapi/fastapi/discussions/13277#discussioncomment-11997827",
    "created_at": "2025-01-29T16:43:18Z",
    "upvotes": 1,
    "author": "Trinkes",
    "author_association": "NONE",
    "is_maintainer": false
  }
}