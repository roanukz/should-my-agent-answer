{
  "number": 13877,
  "title": "Usage of BeforeValidator in FastAPI",
  "body": "### First Check\n\n- [X] I added a very descriptive title here.\n- [X] I used the GitHub search to find a similar question and didn't find it.\n- [X] I searched the FastAPI documentation, with the integrated search.\n- [X] I already searched in Google \"How to X in FastAPI\" and didn't find any information.\n- [X] I already read and followed all the tutorial in the docs and didn't find an answer.\n- [X] I already checked if it is not related to FastAPI but to [Pydantic](https://github.com/pydantic/pydantic).\n- [X] I already checked if it is not related to FastAPI but to [Swagger UI](https://github.com/swagger-api/swagger-ui).\n- [X] I already checked if it is not related to FastAPI but to [ReDoc](https://github.com/Redocly/redoc).\n\n### Commit to Help\n\n- [x] I commit to help with one of those options 👆\n\n### Example Code\n\n```python\nfrom enum import StrEnum\r\nfrom typing import Annotated\r\n\r\nfrom fastapi import FastAPI, Query\r\nfrom pydantic import BaseModel, BeforeValidator\r\n\r\napp = FastAPI()\r\n\r\n\r\nclass Order(StrEnum):\r\n    ASC = \"asc\"\r\n    DESC = \"desc\"\r\n\r\n\r\nclass Sorting(BaseModel):\r\n    field: str\r\n    order: Order\r\n\r\n\r\ndef _validate_sorting(raw_sorting: str) -> Sorting:\r\n    field, order = raw_sorting.split(':')\r\n\r\n    return Sorting.model_validate(field=field, order=order)\r\n\r\n\r\nclass Pool(BaseModel):\r\n    name: str\r\n    volume: int\r\n\r\n\r\n@app.get(\"/pools\")\r\nasync def get_pools(\r\n    sorting: Annotated[\r\n        Sorting,\r\n        BeforeValidator(func=_validate_sorting, json_schema_input_type=str),\r\n        Query()\r\n    ] = Sorting(field=\"volume\", order=Order.ASC),\r\n) -> list[Pool]:\r\n    pools = [Pool(name=\"small_pool\", volume=10), Pool(name=\"big_pool\", volume=20)]\r\n\r\n    return sorted(pools, key=sorting.field, reverse=sorting.order == Order.DESC)\n```\n\n\n### Description\n\nIn the [FastAPI tutorial](https://fastapi.tiangolo.com/tutorial/query-params-str-validations/#custom-validation), there is a mention of using functional validators—specifically AfterValidator and BeforeValidator. The usage of AfterValidator is straightforward, but the purpose and behavior of BeforeValidator in FastAPI are less clear.\r\n\r\nIn Pydantic, BeforeValidator can be used to coerce or cast an input type into a different output type. For this purpose, it provides the json_schema_input_type parameter. This could theoretically be leveraged in FastAPI like so:\r\n```python\r\n@app.get(\"/pools\")\r\nasync def get_pools(\r\n    sorting: Annotated[\r\n        Sorting,\r\n        BeforeValidator(func=_validate_sorting, json_schema_input_type=str),\r\n        Query(),\r\n    ] = Sorting(field=\"volume\", order=Order.ASC),\r\n) -> list[Pool]:\r\n...\r\n```\r\nIn this case, the idea is to expose the parameter as a string in Swagger (via json_schema_input_type) while internally validating and converting it into a Sorting object. However, this doesn’t seem to work. Instead it shows this:\r\n<img width=\"584\" alt=\"image\" src=\"https://github.com/user-attachments/assets/3590d74a-a4c7-4cfb-b124-5a3c1d79c3ac\" />\r\n\r\nI understand that similar behavior can be achieved using Depends to handle custom parsing and validation. But if that’s the case, are there any practical use cases for using BeforeValidator directly in FastAPI routes?\r\n\r\n\n\n### Operating System\n\nmacOS\n\n### Operating System Details\n\n_No response_\n\n### FastAPI Version\n\n0.115.14\n\n### Pydantic Version\n\n2.11.7\n\n### Python Version\n\n3.12.3\n\n### Additional Context\n\n_No response_",
  "body_text": "First Check\n\n I added a very descriptive title here.\n I used the GitHub search to find a similar question and didn't find it.\n I searched the FastAPI documentation, with the integrated search.\n I already searched in Google \"How to X in FastAPI\" and didn't find any information.\n I already read and followed all the tutorial in the docs and didn't find an answer.\n I already checked if it is not related to FastAPI but to Pydantic.\n I already checked if it is not related to FastAPI but to Swagger UI.\n I already checked if it is not related to FastAPI but to ReDoc.\n\nCommit to Help\n\n I commit to help with one of those options 👆\n\nExample Code\nfrom enum import StrEnum\nfrom typing import Annotated\n\nfrom fastapi import FastAPI, Query\nfrom pydantic import BaseModel, BeforeValidator\n\napp = FastAPI()\n\n\nclass Order(StrEnum):\n    ASC = \"asc\"\n    DESC = \"desc\"\n\n\nclass Sorting(BaseModel):\n    field: str\n    order: Order\n\n\ndef _validate_sorting(raw_sorting: str) -> Sorting:\n    field, order = raw_sorting.split(':')\n\n    return Sorting.model_validate(field=field, order=order)\n\n\nclass Pool(BaseModel):\n    name: str\n    volume: int\n\n\n@app.get(\"/pools\")\nasync def get_pools(\n    sorting: Annotated[\n        Sorting,\n        BeforeValidator(func=_validate_sorting, json_schema_input_type=str),\n        Query()\n    ] = Sorting(field=\"volume\", order=Order.ASC),\n) -> list[Pool]:\n    pools = [Pool(name=\"small_pool\", volume=10), Pool(name=\"big_pool\", volume=20)]\n\n    return sorted(pools, key=sorting.field, reverse=sorting.order == Order.DESC)\nDescription\nIn the FastAPI tutorial, there is a mention of using functional validators—specifically AfterValidator and BeforeValidator. The usage of AfterValidator is straightforward, but the purpose and behavior of BeforeValidator in FastAPI are less clear.\nIn Pydantic, BeforeValidator can be used to coerce or cast an input type into a different output type. For this purpose, it provides the json_schema_input_type parameter. This could theoretically be leveraged in FastAPI like so:\n@app.get(\"/pools\")\nasync def get_pools(\n    sorting: Annotated[\n        Sorting,\n        BeforeValidator(func=_validate_sorting, json_schema_input_type=str),\n        Query(),\n    ] = Sorting(field=\"volume\", order=Order.ASC),\n) -> list[Pool]:\n...\nIn this case, the idea is to expose the parameter as a string in Swagger (via json_schema_input_type) while internally validating and converting it into a Sorting object. However, this doesn’t seem to work. Instead it shows this:\n\nI understand that similar behavior can be achieved using Depends to handle custom parsing and validation. But if that’s the case, are there any practical use cases for using BeforeValidator directly in FastAPI routes?\nOperating System\nmacOS\nOperating System Details\nNo response\nFastAPI Version\n0.115.14\nPydantic Version\n2.11.7\nPython Version\n3.12.3\nAdditional Context\nNo response",
  "created_at": "2025-07-08T20:38:34Z",
  "url": "https://github.com/fastapi/fastapi/discussions/13877",
  "answered": true,
  "answer": {
    "body": "Yes, FastAPI is quite straightforward here. If the type of parameter is Pydantic model and it's annotated with `Query`, it treats every field of that model as separate query parameter.\r\nI think we should consider this as a limitation.\r\n\r\nI think something like the code below should work for you:\r\n\r\n```python\r\nfrom enum import StrEnum\r\nfrom typing import Annotated\r\n\r\nfrom fastapi import Depends, FastAPI\r\nfrom pydantic import BaseModel, BeforeValidator\r\nfrom pydantic.json_schema import SkipJsonSchema\r\n\r\napp = FastAPI()\r\n\r\n\r\nclass Order(StrEnum):\r\n    ASC = \"asc\"\r\n    DESC = \"desc\"\r\n\r\n\r\nclass Sorting(BaseModel):\r\n    field: str\r\n    order: Order\r\n\r\n\r\nclass Pool(BaseModel):\r\n    name: str\r\n    volume: int\r\n\r\n\r\ndef _validate_sorting(raw_sorting: str) -> Sorting:\r\n    field, order = raw_sorting.split(\":\")\r\n    return Sorting(field=field, order=order)\r\n\r\n\r\nasync def get_sorting(\r\n    sorting: Annotated[\r\n        str | SkipJsonSchema[Sorting], BeforeValidator(_validate_sorting)\r\n    ],\r\n) -> Sorting:\r\n    assert isinstance(sorting, Sorting)\r\n    return sorting\r\n\r\n\r\n@app.get(\"/pools\")\r\nasync def get_pools(\r\n    sorting: Annotated[Sorting, Depends(get_sorting)] = Sorting(\r\n        field=\"volume\", order=Order.ASC\r\n    ),\r\n) -> list[Pool]:\r\n    pools = [Pool(name=\"small_pool\", volume=10), Pool(name=\"big_pool\", volume=20)]\r\n    return sorted(\r\n        pools,\r\n        key=lambda x: getattr(x, sorting.field),\r\n        reverse=(sorting.order == Order.DESC),\r\n    )\r\n```\r\n\r\nYou can get rid of `get_sorting` dependency, but you will have to check the type of `sorting` in your endpoint with `isinstance(sorting, Sorting)` then",
    "body_text": "Yes, FastAPI is quite straightforward here. If the type of parameter is Pydantic model and it's annotated with Query, it treats every field of that model as separate query parameter.\nI think we should consider this as a limitation.\nI think something like the code below should work for you:\nfrom enum import StrEnum\nfrom typing import Annotated\n\nfrom fastapi import Depends, FastAPI\nfrom pydantic import BaseModel, BeforeValidator\nfrom pydantic.json_schema import SkipJsonSchema\n\napp = FastAPI()\n\n\nclass Order(StrEnum):\n    ASC = \"asc\"\n    DESC = \"desc\"\n\n\nclass Sorting(BaseModel):\n    field: str\n    order: Order\n\n\nclass Pool(BaseModel):\n    name: str\n    volume: int\n\n\ndef _validate_sorting(raw_sorting: str) -> Sorting:\n    field, order = raw_sorting.split(\":\")\n    return Sorting(field=field, order=order)\n\n\nasync def get_sorting(\n    sorting: Annotated[\n        str | SkipJsonSchema[Sorting], BeforeValidator(_validate_sorting)\n    ],\n) -> Sorting:\n    assert isinstance(sorting, Sorting)\n    return sorting\n\n\n@app.get(\"/pools\")\nasync def get_pools(\n    sorting: Annotated[Sorting, Depends(get_sorting)] = Sorting(\n        field=\"volume\", order=Order.ASC\n    ),\n) -> list[Pool]:\n    pools = [Pool(name=\"small_pool\", volume=10), Pool(name=\"big_pool\", volume=20)]\n    return sorted(\n        pools,\n        key=lambda x: getattr(x, sorting.field),\n        reverse=(sorting.order == Order.DESC),\n    )\nYou can get rid of get_sorting dependency, but you will have to check the type of sorting in your endpoint with isinstance(sorting, Sorting) then",
    "url": "https://github.com/fastapi/fastapi/discussions/13877#discussioncomment-13737548",
    "created_at": "2025-07-12T07:31:51Z",
    "upvotes": 2,
    "author": "YuriiMotov",
    "author_association": "MEMBER",
    "is_maintainer": true
  }
}